// Define a function

/*
function sayHello1() {
  console.log('Hello World');
}

function sayHello2() {
  console.log('Hello People');
}

function sayHello3() {
  console.log('Hello Bayan');
}


sayHello2();
sayHello3();

// Invoke, execute or call a function









function add(num1, num2) {
  console.log(num1 + num2);
}

add(5, 10);

*/




// Returning a value
function subtract(num1, num2) {
  
  console.log('After the return');
  return num1 - num2;
 
}

// Assign the returned value to a variable
const result = subtract(10, 2);

console.log(result, subtract(20, 5));










/*


// Returning a value
function subtract(num1, num2) {
  return num1 - num2;

  console.log('After the return');
}

// Assign the returned value to a variable
const result = subtract(10, 2);

console.log(result, subtract(20, 5));







// What is Invoke in javascript?
// In JavaScript, "invoke" refers to the act of calling or executing a function. When you invoke a function, 
// you are telling the JavaScript engine to run the code defined within that function. 
// This is done by using the function's name followed by parentheses.

// Define a function with parameters
function add(num1, num2) {
  console.log(num1 + num2);
}

// Invoke function with arguments
add(5, 10);

// Parameters vs. Arguments
// Parameters are the names of the variables that are used to pass data into a function.
// Arguments are the values that are passed into the function

// Returning a value
function subtract(num1, num2) {
  return num1 - num2;

  console.log('After the return');
}

// Assign the returned value to a variable
const result = subtract(10, 2);

console.log(result, subtract(20, 5));

*/